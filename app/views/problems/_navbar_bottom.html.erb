<nav id="navbar-bottom" class="navbar navbar-fixed-bottom ace-it__navbar--button">
  <div class="row">
    <div class="ace-it__button-col">
      <center>
        <a href="<%= lenses_problem_path(id: @problem.id, lense: 'adaptability') %>"
          class="ace-it__button <%= 'ace-it__button--active' if request.fullpath.include? 'adaptability' %>">
          A
        </a>
      </center>
    </div>
    <div class="ace-it__button-col">
      <center>
        <a href="<%= lenses_problem_path(id: @problem.id, lense: 'cultural_competence') %>"
          class="ace-it__button ace-it__button
          <%= 'ace-it__button--active' if request.fullpath.include? 'cultural' %>
          <%= 'disabled-link' unless @problem.adaptability %> ">
          C
        </a>
      </center>
    </div>
    <div class="ace-it__button-col">
      <center>
        <a href="<%= lenses_problem_path(id: @problem.id, lense: 'empathy') %>"
         class="ace-it__button
         <%= 'ace-it__button--active' if request.fullpath.include? 'empathy' %>
         <%= 'disabled-link' unless @problem.cultural_competence %>" >
         E
        </a>
      </center>
    </div>
    <div class="ace-it__button-col">
      <center>
        <a href="<%= lenses_problem_path(id: @problem.id, lense: 'intellectual_curiosity') %>"
          class="ace-it__button
          <%= 'ace-it__button--active' if request.fullpath.include? 'intellectual' %>
          <%= 'disabled-link' unless @problem.empathy %>" >
          I
        </a>
      </center>
    </div>
    <div class="ace-it__button-col">
      <center>
        <a href="<%= lenses_problem_path(id: @problem.id, lense: 'thinking') %>"
          class="ace-it__button
          <%= 'ace-it__button--active' if request.fullpath.include? 'thinking' %>
          <%= 'disabled-link' unless @problem.intellectual_curiosity %>" >
          T
        </a>
      </center>
    </div>
  </div>
</nav>
